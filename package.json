{
    "name": "three-csg",
    "version": "0.0.1",
    "description": "CSG (Constructive Solid Geometry) library for three.js",
    "repository": "https://github.com/SebiTimeWaster/three-csg",
    "main": "src/main.js",
    "scripts": {
        "build": "node node_modules/rollup/dist/bin/rollup -c",
        "patch": "git checkout master && git pull origin master && npm run build && npm version patch",
        "minor": "git checkout master && git pull origin master && npm run build && npm version minor",
        "major": "git checkout master && git pull origin master && npm run build && npm version major"
    },
    "keywords": [
        "csg",
        "parametric",
        "modeling",
        "threejs",
        "three.js",
        "three"
    ],
    "license": "MIT",
    "devDependencies": {
        "rollup": "^2.10.9",
        "rollup-plugin-terser": "^6.1.0",
        "terser": "^4.7.0"
    }
}
